<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_background"
    android:clipToOutline="true">

    <!-- Custom Background Image -->
    <ImageView
        android:id="@+id/widget_background_image"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"
        android:visibility="gone" />

    <!-- Color Overlay with opacity/transparency -->
    <View
        android:id="@+id/widget_background_overlay"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:visibility="gone" />

    <!-- Content Container -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:padding="16dp"
        android:gravity="center_vertical">

        <!-- Quote mark decoration -->
        <TextView
            android:id="@+id/widget_quote_mark"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="\u201C"
            android:textSize="40sp"
            android:textColor="#80FFFFFF"
            android:fontFamily="serif"
            android:lineSpacingExtra="-12dp"
            android:layout_marginBottom="-8dp" />

        <!-- Quote text -->
        <TextView
            android:id="@+id/widget_quote_text"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:text="Cargando frase..."
            android:textColor="#FFFFFFFF"
            android:textSize="14sp"
            android:lineSpacingMultiplier="1.3"
            android:ellipsize="end"
            android:maxLines="5" />

        <!-- Bottom row: author + refresh button -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_marginTop="8dp">

            <TextView
                android:id="@+id/widget_quote_author"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text=""
                android:textColor="#CCE0E0E0"
                android:textSize="12sp"
                android:fontFamily="sans-serif-medium"
                android:ellipsize="end"
                android:maxLines="1" />

            <ImageButton
                android:id="@+id/widget_refresh_button"
                android:layout_width="32dp"
                android:layout_height="32dp"
                android:src="@android:drawable/ic_popup_sync"
                android:background="?android:attr/selectableItemBackgroundBorderless"
                android:contentDescription="Cambiar frase"
                android:tint="#CCE0E0E0"
                android:padding="4dp" />

        </LinearLayout>

    </LinearLayout>

</FrameLayout>
